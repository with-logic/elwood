# Codex 0.142.5 composer capture

Captured on 2026-09-19 from `@openai/codex@0.142.5` in a real 100×30 PTY,
rendered through Elwood's headless terminal. Version output was `codex-cli 0.142.5`.
The session used an isolated configuration, model `gpt-5.5`, high reasoning effort,
and `tui.status_line = ["model-with-reasoning"]`. No model prompt was submitted.
The project path was replaced with `/tmp/elwood-composer-CAPTURE/project`; trailing
empty terminal rows were removed. The native placeholder was `Implement {feature}`.

Codex chooses among eight composer placeholders. The complete list is in the
[version-tagged upstream source](https://github.com/openai/codex/blob/rust-v0.142.5/codex-rs/tui/src/chatwidget.rs#L1921).
